# reacquisition_interfaces

ROS 2 message/interface package placeholder.

This is the only package that may keep minimal ROS message-generation
build files when a task card asks for them. It must not contain core
algorithm code.
